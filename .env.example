# ── UniFi Gateway ──────────────────────────────────────────
# Required: your UniFi OS gateway credentials
UNIFI_HOST=192.168.1.1
UNIFI_USERNAME=admin
UNIFI_PASSWORD=your_password_here
UNIFI_SITE=default
# UNIFI_PORT=443

# ── Web Dashboard ─────────────────────────────────────────
WEB_HOST=0.0.0.0
WEB_PORT=8080

# ── NetFlow/IPFIX Collector ───────────────────────────────
# Set to false if you don't use NetFlow (dashboard still works via API polling)
NETFLOW_ENABLED=true
NETFLOW_PORT=2055

# ── Data ──────────────────────────────────────────────────
# Polling interval in seconds (minimum 5)
POLL_INTERVAL=30
# Data retention in hours (default 7 days)
RETENTION_HOURS=168
# SQLite database path (relative to working directory)
# DB_PATH=data/monitor.db

# ── Alerts ───────────────────────────────────────────────
# Webhook URL for alert notifications (Discord, Slack, ntfy, etc.)
# Leave empty to disable alerts.
# ALERT_WEBHOOK_URL=https://discord.com/api/webhooks/...
# Minimum seconds between repeated alerts for the same rule (default 300)
# ALERT_COOLDOWN=300
